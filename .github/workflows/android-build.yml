name: Android CI Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --max_old_space_size=4096

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Build web (production)
        run: npm run build

      - name: Install Capacitor and sync
        run: |
          npx cap sync android
          npx cap copy

      - name: Setup Java (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          target: default
          arch: x86_64
          force-avd-creation: false

      - name: Build APK (debug)
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

      # --- Release signing (optional) ---
      # To enable signed release builds place your keystore as a base64 secret
      # named `KEYSTORE_BASE64` and add the following secrets: `KEYSTORE_PASSWORD`,
      # `KEY_ALIAS`, `KEY_PASSWORD`.
      # The steps below decode the keystore into the android project, create
      # a `keystore.properties` file consumed by Gradle, and run `assembleRelease`.
      # Uncomment and adapt if you have configured your `android/app/build.gradle`
      # to read signing properties from `keystore.properties` (see android/README_SIGNING.md).

      # - name: Decode keystore and create signing properties
      #   if: secrets.KEYSTORE_BASE64
      #   run: |
      #     mkdir -p android/app
      #     echo "$KEYSTORE_BASE64" | base64 --decode > android/app/release.keystore
      #     cat > android/keystore.properties <<EOF
      #     storeFile=release.keystore
      #     storePassword=${{ secrets.KEYSTORE_PASSWORD }}
      #     keyAlias=${{ secrets.KEY_ALIAS }}
      #     keyPassword=${{ secrets.KEY_PASSWORD }}
      #     EOF
      #   env:
      #     KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}

      # - name: Build Release APK (signed)
      #   if: secrets.KEYSTORE_BASE64
      #   working-directory: android
      #   run: ./gradlew assembleRelease --no-daemon

      # - name: Upload release APK
      #   if: secrets.KEYSTORE_BASE64
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: app-release-apk
      #     path: android/app/build/outputs/apk/release/app-release.apk

